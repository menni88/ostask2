
on:
  push:
    branchess:
      - master
    



jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: 'login to docker'
        run: docker login --username ${{secrets.DOCKER_USRNM}} --password ${{secrests.TOKEN}}

        
      